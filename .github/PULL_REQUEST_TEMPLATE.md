## What changed

<!-- One paragraph: the behaviour before, the behaviour after, and why. -->

## Checks run

- [ ] `cargo fmt --all --check`
- [ ] `cargo clippy --workspace --all-targets --locked -- -D warnings`
- [ ] `cargo test --workspace --locked` (goldens reviewed, not regenerated blindly)
- [ ] `cargo deny check`
- [ ] `cd desktop && pnpm run lint && pnpm run typecheck && pnpm test && pnpm run build`
- [ ] `python3 docs/development/tools/check_docs.py`
- [ ] `desktop/src/generated.ts` and `generated-labels.json` regenerated by `cargo test -p azdocs-desktop` and committed

## Notes for the reviewer

<!-- Golden diffs worth a look, a live-tenant check that cannot run in CI, a decision that needs a second opinion. -->
