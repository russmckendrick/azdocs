name = "resource_type_counts"
category = "inventory"
kind = "inventory"
description = "Resource counts by type across the estate"
kql = '''
resources
| summarize count = count() by type
| order by count desc, type asc
'''
