name = "resource_type_counts"
category = "inventory"
kind = "inventory"
description = "Resource counts by type across the estate"
kql = '''
resources
| summarize resourceCount = count() by type
| order by resourceCount desc, type asc
'''
